# ===========================================
# Newra News — Global Environment Variables
# ===========================================
# Copy this file to .env and fill in the values.
# Each app also has its own .env.example with app-specific variables.

# Database (used by packages/database)
DATABASE_URL=postgresql://user:password@localhost:5432/newranews
